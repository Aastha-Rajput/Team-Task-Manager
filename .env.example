JWT_SECRET=replace-with-a-long-random-secret
# Optional: omit this locally to use local-data.json instead of MongoDB.
# MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster0.example.mongodb.net/team-task-manager
# Required on Railway:
# NODE_ENV=production
# Optional: when set, users need this value to create an Admin account.
# ADMIN_INVITE_CODE=demo-admin-2026
